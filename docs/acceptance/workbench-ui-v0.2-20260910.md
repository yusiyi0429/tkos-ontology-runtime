# 工作台 UI v0.2 布局独立验收

2026-09-10。本地合成案例范围通过；下列首轮布局验证基线为 `3cd9109`，A1 兼容复核见文末。
实现主体由 Kimi Code 0.40.1 完成；Codex 给出设计方案、审查修正并独立验证。

## 页面结果

目录先呈现业务域和实例列表，类型字段默认收起。桌面 860px 高视口下，首个实例从
原版约 991px 提前至 339px。详情拆为概览、版本与内容、来源关系；三状态仅位于
演练 WorkItem 概览。回执以动作和判断为主，完整审计字段可展开。
Context 左侧输入、右侧结果；新建和回读分开，双时间常显，多份快照用选择器切换。

入口：<http://127.0.0.1:8032/workbench/#/catalog>

## 验证证据

证据位于本地忽略目录 `artifacts/runtime-acceptance/workbench-layout-20260910/`。

| 检查 | 结果 | 记录 |
| --- | --- | --- |
| 前端回归及 JS 语法 | 60/60，16 个模块语法通过 | node-tests.log |
| 原有 GET 联调 | 34/34 | node-get-integration.log |
| 独立严格读取 | 77/77 | strict-read-checks.json |
| 跨页对象与历史版本、目录筛选返回 | 通过 | navigation-browser.json |
| v1/v2 评审、原文、折叠保持与下载 hash | 通过 | delivery-browser.json |
| Context 模式、草稿、名称、双时间、历史选择、角色隔离 | 通过 | context-browser.json |
| 对象/关系真实分页、精确关系跳转、旧角色响应迟到 | 通过 | pagination-race-browser.json |
| 390/850/1200px 四页 | 无横向溢出 | responsive-browser.json |
| 最终首屏位置与键盘展开 | 通过 | final-layout-metrics.json |
| 部署/数据/既有文件保留 | 通过 | final-audit.json |

严格 HTTP 检查要求有权身份的必需读取必须 200；不把 403/404 统算成功。
outsider 的回执详情按既有契约要求 403/FORBIDDEN 且无正文，其余受限读取要求 404。

浏览器只显式生成了 1 条历史审计快照；切模式、编辑条件、切换历史选择均未 POST。
17 张受检表仅 `gov_context_snapshots` 增加 1 条，另外 16 张不变。14 个既有容器的
ID、镜像和启动时间不变，24 个既有无关文件保持原 hash。19 个部署公开文件与源码一致。

## 范围

本次是本地工作台布局和导航验证，不涉及生产部署、Clark 或业务状态写接口。
角色选择器是合成演练；正式登录、真实企业数据、实时撤权刷新、完整读屏器与容量未验收。

## A1 主线兼容复核（2026-09-10）

在主线 `e9b904a` 加本次工作台改动上重新验证。复用专用 `tkos_a1_*` 验证库，
新建独立合成 scope；既有演示数据库仍停在 0017，没有升级原有容器。
Codex 的本地编排器仅选择验收环境，复用原始 API、Scenario、读取断言与前端模块。

- Kimi Code 0.42.0 补齐 `control_plane_only` 的「仅治理登记」文案、
  ProtocolSentinel hash 深链接，以及无公开 schema 时的准确说明。
- Codex 更新独立验收器：目录严格检查 10 个业务类型加 1 个治理哨兵；
  对象摘要、责任人和分页顶层的 A1 协议信息严格匹配既有 v0.2 注册，保留原始 DRI 含义。
  GET 联调不再固定旧证据 ID；根据当前案例核对原始字节 SHA-256，有权人遇到 403/404 必须失败。
- 最终验证：Node 61/61、Python 工作台 70/70、独立 HTTP 11/11 组、真实 GET 34/34。
- Ego 实际页面验证：v1/v2 内容、三项独立状态、跨页当前对象、回执详情、Context 模式草稿、
  显式生成与历史回读、身份切换清空旧内容、A1 哨兵深链接全部通过。
  四页在 390/850/1200px 下共 12 次检查无页面横向溢出，并查看桌面和手机截图。
- SQL 比较覆盖 22 表，仅 `gov_context_snapshots` 变化；浏览器明确触发 1 次快照 POST，
  其余 21 表保持不变。浏览器操作没有执行交付、成果或反馈业务写入。

前两次独立 HTTP 检查因旧验收器的类型数量和字段集合预期过时而失败，记录保留；
最终结论来自修正后 `workbench-a1-20260910-r3` 的同一次完整执行。
本记录证明本地 A1 兼容，不代表远程发布。源文件 hash 和结果见
[结构化复核记录](workbench-ui-a1-20260910.json)。提交与推送结果以 Git 记录为准。
